# Project local artifacts
/.artifacts/
/.notes
/.temp/

# Secrets and local config
.env
.env.*
!.env.sample

# Python local artifacts
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/
ENV/
.python-version
__pypackages__/

# PDM
.pdm-python
.pdm-build/
pdm.lock

# Node artifacts
node_modules/

# Build outputs
build/
dist/
target/
.build/
_obj/
prebuilds/
.zig-cache/
zig-cache/
zig-out/

# Native binaries and generated parser artifacts
*.a
*.so
*.so.*
*.dylib
*.dll
*.pc
*.exp
*.lib

# Grammar volatiles
*.wasm
*.obj
*.o
*.egg-info/
*.whl

# Generated examples and archives
/examples/*/
!/examples/playground/
!/examples/playground/**
*.tar.gz
*.tgz
*.zip

# Packed ITS snapshot: nested Russian paths exceed Windows MAX_PATH under
# Cargo's git checkout (`~/.cargo/git/checkouts/...`). Unpack locally if needed.
/spec/sdbl-syntax/
!/spec/sdbl-syntax.zip

# Test and tool caches
.pytest_cache/
.ruff_cache/

# OS files
Thumbs.db
.DS_Store

# Local implementation prompts
scripts/infr/

# agent-rules:begin | управляется sync-agent-rules.py, правьте dev-utils/agent-rules/
# .claude/skills/<name> — символические ссылки на .agents/skills/<name>. Канон
# лежит в .agents/skills и коммитится, ссылки раскладывает sync-agent-rules.py
# на каждой машине. В git они не хранятся: симлинк восстанавливается при
# checkout только там, где включён core.symlinks, а это настройка установщика,
# а не репозитория.
.claude/skills/
# .claude/worktrees/<name> — worktree, которые создаёт Claude Code. Это полные
# копии репозитория со своим .git: без записи git status в основной копии
# показывает их как неотслеживаемые, а git add . добавляет как вложенный репозиторий.
.claude/worktrees/
# agent-rules:end
